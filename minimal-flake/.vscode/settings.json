{
  // EDITOR SETTINGS
  // What this actually does depends on the language server configuration.
  "editor.formatOnSave": true,
  "editor.inlayHints.enabled": "off",
  "editor.wordWrap": "on",
  "editor.tabSize": 2,
  // NIX LANGUAGE SUPPORT
  "nix.enableLanguageServer": true,
  "nix.serverPath": "nixd",
  // LSP config can be passed via the ``nix.serverSettings.{lsp}`` as shown below.
  "nix.serverSettings": {
    // Settings for 'nixd' LSP
    "nixd": {
      "nixpkgs": {
        // Nixd provides package, lib completion/information from here.
        "expr": "import (builtins.getFlake ../).inputs.nixpkgs { }"
      },
      "formatting": {
        "command": [
          "nix",
          "fmt"
        ]
      },
    }
  }
}